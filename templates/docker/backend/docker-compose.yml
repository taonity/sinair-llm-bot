services:
  backend:
    image: ${DOCKER_REGISTRY:-local}/sinair-llm-bot-backend:latest
    mem_limit: ${BACKEND_MEMORY_LIMIT:-768m}
    environment:
      JDK_JAVA_OPTIONS: --add-opens java.base/java.lang=ALL-UNNAMED
      JAVA_TOOL_OPTIONS: >-
        -XX:InitialRAMPercentage=${BACKEND_INITIAL_RAM_PERCENTAGE:-10.0}
        -XX:MaxRAMPercentage=${BACKEND_MAX_RAM_PERCENTAGE:-60.0}
        ${BACKEND_JAVA_EXTRA_OPTIONS:-}
      GRAFANA_MCP_CONTAINER_PREFIX: ${COMPOSE_PROJECT_NAME}
#      LOGGING_LEVEL_ROOT: TRACE
#      LOGGING_LEVEL_ORG_TAONITY_SINAIRLLMBOT: DEBUG
    command: --spring.profiles.active=$SPRING_PROFILES_ACTIVE

    env_file:
      - ../${COMPOSE_ENV_FILE:-.env}
    volumes:
      - ./config:/sinair-llm-bot/config
    healthcheck:
      test: curl localhost:8080/actuator/health || exit 1
      interval: 10s
      timeout: 10s
      retries: 100