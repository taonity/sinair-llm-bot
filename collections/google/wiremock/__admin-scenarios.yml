info:
  name: /__admin/scenarios
  type: http
  seq: 1

http:
  method: GET
  url: "{{wiremockBaseUrl}}/__admin/scenarios"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
