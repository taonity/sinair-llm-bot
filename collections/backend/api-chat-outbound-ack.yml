info:
  name: /api/chat/outbound/ack
  type: http
  seq: 11

http:
  method: POST
  url: "{{backendBaseUrl}}/api/chat/outbound/ack"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "ids": ["1", "2"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
