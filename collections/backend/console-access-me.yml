info:
  name: /console/access/me
  type: http
  seq: 13

http:
  method: GET
  url: "{{backendBaseUrl}}/console/access/me"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
