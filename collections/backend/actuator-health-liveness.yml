info:
  name: /actuator/health/liveness
  type: http
  seq: 3

http:
  method: GET
  url: "{{backendBaseUrl}}/actuator/health/liveness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
