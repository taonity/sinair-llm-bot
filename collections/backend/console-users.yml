info:
  name: /console/users
  type: http
  seq: 18

http:
  method: GET
  url: "{{backendBaseUrl}}/console/users"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
