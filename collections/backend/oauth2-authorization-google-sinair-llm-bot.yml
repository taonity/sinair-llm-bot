info:
  name: /oauth2/authorization/google-sinair-llm-bot
  type: http
  seq: 4

http:
  method: GET
  url: "{{backendBaseUrl}}/oauth2/authorization/google-sinair-llm-bot"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
