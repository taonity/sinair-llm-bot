info:
  name: /console/chat-events/:id
  type: http
  seq: 25

http:
  method: DELETE
  url: "{{backendBaseUrl}}/console/chat-events/1"
  headers:
    - name: Cookie
      value: "{{cookies}}"
    - name: X-XSRF-TOKEN
      value: "{{xsrfToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
