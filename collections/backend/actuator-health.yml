info:
  name: /actuator/health
  type: http
  seq: 2

http:
  method: GET
  url: "{{backendBaseUrl}}/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
