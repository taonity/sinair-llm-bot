info:
  name: /dev/stub-users
  type: http
  seq: 32

http:
  method: GET
  url: "{{backendBaseUrl}}/dev/stub-users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
