info:
  name: /console/access/requests
  type: http
  seq: 15

http:
  method: GET
  url: "{{backendBaseUrl}}/console/access/requests"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
