info:
  name: /actuator/info
  type: http
  seq: 1

http:
  method: GET
  url: "{{backendBaseUrl}}/actuator/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
