info:
  name: /api/chat/outbound (claim)
  type: http
  seq: 10

http:
  method: GET
  url: "{{backendBaseUrl}}/api/chat/outbound?limit=10"
  params:
    - name: room
      value: ""
      disabled: true
    - name: limit
      value: "10"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
