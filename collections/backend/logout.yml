info:
  name: /logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{backendBaseUrl}}/logout"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
