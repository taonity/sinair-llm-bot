info:
  name: /actuator/prometheus
  type: http
  seq: 7

http:
  method: GET
  url: "{{backendBaseUrl}}/actuator/prometheus"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
