info:
  name: /api/chat/outbound/presence
  type: http
  seq: 12

http:
  method: GET
  url: "{{backendBaseUrl}}/api/chat/outbound/presence"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
