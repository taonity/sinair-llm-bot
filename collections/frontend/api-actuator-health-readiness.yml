info:
  name: /api/actuator/health/readiness
  type: http
  seq: 3

http:
  method: GET
  url: "{{frontendUrl}}/api/actuator/health/readiness"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
