info:
  name: /api/actuator/health
  type: http
  seq: 2

http:
  method: GET
  url: "{{frontendUrl}}/api/actuator/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
