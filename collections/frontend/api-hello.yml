info:
  name: /api/hello
  type: http
  seq: 10

http:
  method: GET
  url: "{{frontendUrl}}/api/hello"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
