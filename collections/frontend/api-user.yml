info:
  name: /api/user
  type: http
  seq: 11

http:
  method: GET
  url: "{{frontendUrl}}/api/user"
  headers:
    - name: Cookie
      value: "{{cookies}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
