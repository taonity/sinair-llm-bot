info:
  name: /api/dev/stub-users
  type: http
  seq: 13

http:
  method: GET
  url: "{{frontendUrl}}/api/dev/stub-users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
